export default function Home() {
  return (
    <main className="p-8">
      <h1 className="text-2xl font-semibold">Foundry</h1>
      <p className="mt-2 text-sm opacity-70">Scaffold ready. Run <code>npm run sync</code> once the token is in .env.</p>
    </main>
  );
}
